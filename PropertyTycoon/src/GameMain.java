import javax.swing.*;
import java.io.BufferedInputStream;
import java.io.InputStream;
import static java.lang.System.in;
import java.util.Scanner;

/**
 *
 * @author Kieran
 */
public class GameMain {



    /**public static void main(String[] args){

        /**Scanner userInput = new Scanner(System.in);

        *System.out.println("Select game type. (Classic or Abridged).");
        *String gameType = userInput.next();

        *System.out.println("Select number of human players. (1-6. Max 6 human & CPU players.)");
        *int humanPlayers = userInput.nextInt();

        *Used to select how many CPU players.
        *System.out.println("Select number of CPU players. (1-6. Max 6 human & CPU players.)");
        *int CPUPlayers = userInput.nextInt();


        *if(gameType.equals("Classic")){
        *    ClassicGame game = new ClassicGame(humanPlayers, CPUPlayers);
        *    game.initPlayers(humanPlayers);
        *} else if(gameType.equals("Abridged")){
        *    AbridgedGame game = new AbridgedGame(humanPlayers, CPUPlayers);
        *}
        *
    }*/

}
